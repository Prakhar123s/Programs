#include<stdio.h>
void main()
{
    float radius, aor;
    printf("Enter the radius of circle:");
    scanf("%f",&radius);
    aor=2*3.14*radius;
    printf("The area of radius is :%2f",aor);
}